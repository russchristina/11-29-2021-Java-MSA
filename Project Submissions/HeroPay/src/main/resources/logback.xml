<?xml version="1.0" encoding="UTF-8" ?>

<configuration>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Specify the location of the file you're logging to -->
        <file>planetMartDebug.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>logFile.%d{yyyy-MM-dd}.log</fileNamePattern>

            <!-- keep 30 days' worth of history capped at 3GB total size -->
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>

        </rollingPolicy>

        <!-- This encoder is determining what the format of our logging messages will be. -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE2" class="ch.qos.logback.core.FileAppender">
        <!-- Specify the location of the file you're logging to -->
        <file>planetMartTransaction.log</file>

        <!-- This encoder is determining what the format of our logging messages will be. -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE3" class="ch.qos.logback.core.FileAppender">
        <!-- Specify the location of the file you're logging to -->
        <file>planetMartError.log</file>

        <!-- This encoder is determining what the format of our logging messages will be. -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="debugLogger" level ="DEBUG">
        <appender-ref ref="FILE"/>
    </logger>

    <logger name="transactionLogger" level ="INFO">
        <appender-ref ref="FILE2"/>
    </logger>

    <logger name="errorLogger" level ="ERROR">
        <appender-ref ref="FILE3"/>
    </logger>
</configuration>