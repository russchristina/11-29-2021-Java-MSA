// Write a method that takes any number of integers and returns the sum of all of those integers

public class Main {
	
	//Take any number of integers
	//Var...args method that can deal with any amount of input as long as same type

	public static void main(String[] args) {
		
		//creating an array as a placeholder for data

		int[] numbers = {3, 4, 6, 213, 2, 3};
		Main m = new Main();

		int sum = m.addInts(numbers);
		System.out.printf("Your sum is: %d", sum);
	}
	

	public int addInts(int...args) {
		
		int sum = 0;

		for(int i: args) {
			sum += i;
		}
		return sum;
	}
}
